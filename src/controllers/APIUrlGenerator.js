const APIUrlGenerator = (categories, triviaCriteria) => {
  let baseURL = 'https://opentdb.com/api.php?';
  const keyCriterias = Object.keys(triviaCriteria);
  console.log(keyCriterias)
}

export default APIUrlGenerator;